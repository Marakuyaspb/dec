/* ROOT*/
const ROOT_PRODUCTS_CORNER = document.getElementById('goods_show_arm');
const ROOT_SPINNER_CORNER = document.getElementById('spinner_arm');
const ROOT_ERROR_CORNER = document.getElementById('error_arm');
